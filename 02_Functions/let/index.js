var test = function()
{
  var nCounter = 19;
  for( let nCounter = 0; nCounter < 10; nCounter++){
    console.log( 'Inside the loop:', nCounter );
  }
  console.log( 'Out of the loop:', nCounter );
};